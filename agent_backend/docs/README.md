# unified_workspace_backend
# Pip install the below requirements before running the app
# pip install openai
# pip install fastmcp
# google-auth google-auth-oauthlib google-auth-httplib2
# pip install google-api-python-client
# pip install  pytz tzlocal
# run the app.py file to start the services 
# Only 5173 port is given access to the sevice, so please maintain the UI port same
# pip install openai
# pip install fastmcp
# google-auth google-auth-oauthlib google-auth-httplib2
# pip install google-api-python-client
# pip install local_tz
# pip install dateparser
# pip install slack-sdk
